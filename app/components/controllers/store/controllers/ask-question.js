'use strict';
angular.module('store')
.controller('askQuestionCtrl', function ($log, $state, $rootScope, Store) {

  /* Storing contextual this in a variable for easy access */

  var ctrl = this;

});
